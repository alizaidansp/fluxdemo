---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: voting-app-kustomization-demo
  namespace: flux-system
spec:
  interval: 1m0s
  path: ./ #all changes from the mainfest files in the root of the source repo
  prune: true
  sourceRef:
    kind: GitRepository
    name: voting-app-source-demo
